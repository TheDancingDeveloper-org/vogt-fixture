# vogt-fixture
Vogt forge-adapter test fixture (#294). Rebuilt by scripts/fixture_reset.py — do not hand-edit.

## Rebuilding the fixture

1. Clone this repo and install dependencies.
2. Run `python scripts/fixture_reset.py` to regenerate all fixture files from the manifest.
3. Commit the result — every file in this repo is output of that script.

> **Note:** Do not hand-edit generated files. To change the fixture, update the manifest (TODO: link manifest once it lands) and re-run the reset script.
